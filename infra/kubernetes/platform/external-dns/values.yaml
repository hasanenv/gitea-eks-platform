provider:
  name: cloudflare

env:
  - name: CF_API_TOKEN
    valueFrom:
      secretKeyRef:
        name: cloudflare-api-token
        key: api-token

domainFilters:
  - "hasanenv.co.uk"

policy: sync

txtOwnerId: "gitea-eks"

serviceAccount:
  annotations: {}